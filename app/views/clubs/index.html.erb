<div class="home-photo"></div>
<div class="gradient"></div>
<!--<div class="home-infos">
  <%= image_tag 'Goal2.png' %>
  <div class="text-infos">
    <h3>Une nouvelle façon de se procurer des joueurs.</h3>
    <p>Test</p>
    <h1 id="title">NEW MERCATO</h1>
  </div>
  <%= image_tag 'Goal1.png' %>
</div>
<div class="gradient"></div>-->
<div class="container">
  <div class="club-photo-container">
    <% @clubs.each do |club| %>
      <%= link_to club_path(club) do %>
        <%= image_tag club.photo %>
      <% end %>
    <% end %>
  </div>
</div>
